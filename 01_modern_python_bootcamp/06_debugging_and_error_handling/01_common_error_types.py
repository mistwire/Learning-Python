# SyntaxError = incorrect syntax, something Python can't parse

# NameError = occurs when a variable isn't defined

# TypeError = an operation or function is applied to the wrong type (adding str and int)

# IndexError = referencing an element in a list using an invalid index (outside of the range of list or string)

# ValueError = occurs when a built-in operation/function recieves an arg that has the right type but inappropriate value
# int('foo')

# KeyError = when a dict doesn't have a specific key:

# AttributeError - when a variable doesn't have an attribute : 
# "awesome".foo

https://docs.python.org/3/library/exceptions.html

